version: "3.8"

networks:
  app-network:
    driver: bridge

volumes:
  redis_data:

services:
  redis:
    image: redis:7-alpine
    container_name: redis
    restart: unless-stopped
    networks:
      - app-network
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      retries: 5

  app:
    image: juanfranciscofernandez/my-app:latest
    container_name: app
    restart: unless-stopped
    networks:
      - app-network

    depends_on:
      redis:
        condition: service_healthy

    ports:
      - "8087:8087"

    environment:
      SPRING_DATA_REDIS_HOST: redis
      SPRING_DATA_REDIS_PORT: 6379
